/**
 * API pública de `tenants`: lo único que otro módulo puede importar de acá.
 */
export { TenantsModule } from '../infrastructure/modules/tenants.module'
export { TenantsFacade } from './tenants.facade'
