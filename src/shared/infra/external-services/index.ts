/**
 * Clientes HTTP hacia APIs externas.
 *
 * Extendé `ApiService` (`src/shared/infra/base-api.ts`) para cada API que
 * consumas y registrá el servicio acá: `InfraModule` lo provee y lo exporta
 * automáticamente.
 */
export const Services = []
