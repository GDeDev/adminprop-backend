/**
 * Command handlers transversales de infraestructura (no de un feature puntual).
 * Los handlers de cada feature viven en `src/application/<feature>/commands`.
 */
export const CommandHandlers = []
